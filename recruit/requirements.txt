# Nexus CRM — Recruit Service Dependencies
# No APScheduler (no scheduled jobs in Recruit Space)
# No bcrypt (no password hashing — auth is Core only)
# No msal (no MS Graph — Outlook bulk email is Core only)

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.22

# Supabase
supabase>=2.7.0

# Auth (JWT verify only — no password hashing)
PyJWT==2.12.1
python-dotenv==1.2.2

# File handling
pandas==3.0.1
openpyxl==3.1.5

# Email (Resend — onboarding notifications)
resend==2.26.0

# Validation
pydantic==2.12.5
pydantic[email]==2.12.5
email-validator==2.3.0

# HTTP
httpx>=0.27,<1.0.0

# Google Drive — resume storage
google-api-python-client==2.120.0
google-auth>=2.48.1
google-auth-httplib2==0.2.0

# LLM — resume parsing / ATS scoring
# Groq (primary) → Gemini (fallback) → OpenAI → keyword fallback
google-genai>=1.0.0
openai>=1.50.0

# Resume text extraction
pdfplumber>=0.11.0
python-docx>=1.1.2

# Rate limiting — protects /public/apply
slowapi==0.1.9

# Resume masking (phone/email redaction)
PyMuPDF>=1.24.0
