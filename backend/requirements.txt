# Nexus CRM + ATS — Backend Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.22

# Supabase (replaces MongoDB)
supabase==2.4.3

# Auth
bcrypt==4.1.3
PyJWT==2.12.1
python-dotenv==1.2.2

# File import
pandas==3.0.1
openpyxl==3.1.5
xlrd==2.0.2

# Email
resend==2.26.0

# Scheduler (daily digest emails)
apscheduler==3.10.4

# Validation
pydantic==2.12.5
pydantic[email]==2.12.5
email-validator==2.3.0

# HTTP
httpx<=0.28.1

# Google Drive — resume storage
google-api-python-client==2.120.0
google-auth==2.28.2
google-auth-httplib2==0.2.0

# LLM — resume tech-stack extraction (pick one provider, default: gemini)
# Gemini: FREE 1M TPM, 1500 req/day — best free tier
# gemini-2.0-flash-lite: 30 RPM (2x faster burst than 1.5-flash)
google-generativeai>=0.8.0
# Alternatives (optional):
# anthropic>=0.40.0
# openai>=1.50.0

# Resume text extraction
pdfplumber>=0.11.0
python-docx>=1.1.2