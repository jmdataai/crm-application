# Nexus CRM + ATS — Backend Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.22

# Supabase (replaces MongoDB)
# >=2.7.0 removes the httpx<0.28 cap that conflicted with google-genai
supabase>=2.7.0

# Auth
bcrypt==4.1.3
PyJWT==2.12.1
python-dotenv==1.2.2

# File import
pandas==3.0.1
openpyxl==3.1.5
xlrd==2.0.2

# Email
resend==2.26.0

# Scheduler (daily digest emails)
apscheduler==3.10.4

# Validation
pydantic==2.12.5
pydantic[email]==2.12.5
email-validator==2.3.0

# HTTP (let pip resolve — supabase 2.7+ and google-genai both work with >=0.28)
httpx>=0.27,<1.0.0

# Google Drive — resume storage
google-api-python-client==2.120.0
google-auth>=2.48.1
google-auth-httplib2==0.2.0

# LLM — resume tech-stack extraction + JD parsing
# Provider cascade: Groq → Gemini → OpenAI → Anthropic → keyword fallback
#
# Groq  : 14,400 req/day FREE  — llama-3.3-70b-versatile  (uses openai SDK)
# Gemini: 1,000  req/day FREE  — gemini-2.5-flash-lite     (google-genai SDK)
# OpenAI: paid only            — gpt-4o-mini
# Anthropic: paid only         — claude-haiku-4-5-20251001
#
# NOTE: gemini-2.0-flash is DEPRECATED, retiring June 1 2026.
#
# google-genai is the official SDK (google-generativeai is EOL)
google-genai>=1.0.0
# openai SDK also handles Groq (same SDK, different base_url)
openai>=1.50.0
# Optional paid providers:
# anthropic>=0.40.0

# Resume text extraction
pdfplumber>=0.11.0
python-docx>=1.1.2